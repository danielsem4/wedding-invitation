import { Envelope } from "@/components/Envelope";

export default function Home() {
  return (
    <main className="scene-bg scene-shell relative overflow-x-hidden">
      <div className="relative z-10">
        <Envelope />
      </div>
    </main>
  );
}
